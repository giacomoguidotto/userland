# Local history only. Atuin sync writes an auth session and encryption key to
# files, so it is disabled under the no-plaintext-secrets policy.
auto_sync = false
update_check = false
secrets_filter = true
style = "compact"
