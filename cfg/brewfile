# macOS-only manager and desktop declarations. Portable command-line tools
# remain pinned in cfg/mise.toml so the same declaration works on Linux and
# Termux.
brew "git"
brew "duti"
# btop and eza do not publish macOS release archives; Homebrew is their
# declarative macOS source while Mise owns the pinned Linux installations.
brew "btop"
brew "eza"
cask "1password", args: { adopt: true }
cask "font-jetbrains-mono-nerd-font", args: { adopt: true }
cask "ghostty", args: { adopt: true }
cask "helium-browser", args: { adopt: true }
cask "raycast", args: { adopt: true }
cask "shottr", args: { adopt: true }
cask "spotify", args: { adopt: true }
cask "t3-code", args: { adopt: true }
cask "wispr-flow", args: { adopt: true }
